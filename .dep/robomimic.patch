diff --git a/robomimic/macros.py b/robomimic/macros.py
--- a/robomimic/macros.py
+++ b/robomimic/macros.py
@@ -13,13 +13,13 @@ WANDB_ENTITY = None
 # alternatively, set up wandb from terminal with `wandb login`
 WANDB_API_KEY = None
 
-try:
-    from robomimic.macros_private import *
-except ImportError:
-    from robomimic.utils.log_utils import log_warning
-    import robomimic
-    log_warning(
-        "No private macro file found!"\
-        "\nIt is recommended to use a private macro file"\
-        "\nTo setup, run: python {}/scripts/setup_macros.py".format(robomimic.__path__[0])
-    )
+# try:
+#     from robomimic.macros_private import *
+# except ImportError:
+#     from robomimic.utils.log_utils import log_warning
+#     import robomimic
+#     log_warning(
+#         "No private macro file found!"\
+#         "\nIt is recommended to use a private macro file"\
+#         "\nTo setup, run: python {}/scripts/setup_macros.py".format(robomimic.__path__[0])
+#     )
diff --git a/robomimic/utils/env_utils.py b/robomimic/utils/env_utils.py
--- a/robomimic/utils/env_utils.py
+++ b/robomimic/utils/env_utils.py
@@ -180,8 +180,8 @@ def create_env(
         postprocess_visual_obs=True,
         **kwargs,
     )
-    print("Created environment with name {}".format(env_name))
-    print("Action size is {}".format(env.action_dimension))
+    # print("Created environment with name {}".format(env_name))
+    # print("Action size is {}".format(env.action_dimension))
     return env
 
 
